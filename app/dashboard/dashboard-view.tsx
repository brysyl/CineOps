'use client';

import { useEffect, useState } from 'react';
import { AlertTriangle, ArrowUpRight, CheckCircle2, ChevronRight, CircleGauge, Cpu, Layers3, MemoryStick, Server, Thermometer } from 'lucide-react';
import { jobs, nodeUtilization } from '@/lib/data';

export default function DashboardView() {
  const [tick, setTick] = useState(0);
  useEffect(() => { const timer = window.setInterval(() => setTick((value) => value + 1), 3000); return () => window.clearInterval(timer); }, []);
  const gpu = 78 + (tick % 4);
  return <div className="mx-auto max-w-[1500px] animate-float-in"><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[.2em] text-emerald-400"><span className="h-1.5 w-1.5 rounded-full bg-emerald-400" /> Production live</div><h1 className="text-3xl font-bold tracking-tight text-white sm:text-4xl">Good afternoon, operator.</h1><p className="mt-2 text-sm text-slate-500">Here is the health of your render ecosystem right now.</p></div><div className="flex items-center gap-2 text-xs text-slate-500"><span className="rounded border border-slate-800 bg-slate-900 px-2 py-1 font-mono text-slate-400">STAGE 03</span> Updated just now</div></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={<CircleGauge />} label="GPU cluster utilization" value={`${gpu}.4%`} delta="+4.2% vs last hour" tone="amber" /><Stat icon={<Server />} label="Active render nodes" value="28 / 32" delta="87.5% availability" tone="emerald" /><Stat icon={<MemoryStick />} label="VRAM consumption" value="312.8 GB" delta="of 400 GB allocated" tone="sky" /><Stat icon={<Layers3 />} label="Frames in queue" value="4,860" delta="12 jobs processing" tone="rose" /></section><section className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_.8fr]"><div className="glass rounded-xl p-5"><div className="mb-5 flex items-start justify-between"><div><p className="text-sm font-semibold text-white">Cluster performance</p><p className="mt-1 text-xs text-slate-500">GPU utilization across 32 render nodes</p></div><div className="flex gap-4 text-[10px] font-semibold uppercase tracking-wider text-slate-500"><span className="flex items-center gap-1.5"><i className="h-2 w-2 rounded-full bg-amber-400" />GPU</span><span className="flex items-center gap-1.5"><i className="h-2 w-2 rounded-full bg-emerald-400" />Healthy</span></div></div><div className="grid grid-cols-8 gap-2 sm:grid-cols-16">{nodeUtilization.map((value, i) => <div key={i} className="group relative"><div className="h-24 overflow-hidden rounded-md bg-slate-900"><div className={`absolute inset-x-0 bottom-0 rounded-md transition-all duration-700 ${value > 84 ? 'bg-amber-400' : 'bg-emerald-400/75'}`} style={{ height: `${value}%` }} /></div><p className="mt-2 text-center font-mono text-[9px] text-slate-600">{String(i + 1).padStart(2, '0')}</p><div className="pointer-events-none absolute -top-9 left-1/2 hidden -translate-x-1/2 rounded bg-slate-800 px-2 py-1 text-[10px] text-white group-hover:block">{value}%</div></div>)}</div><div className="mt-5 flex items-center justify-between border-t border-slate-800 pt-4 text-xs text-slate-600"><span>Node utilization / last 5 min</span><span className="font-mono text-slate-400">MIN 58% <span className="mx-2 text-slate-700">•</span> MAX 88%</span></div></div><div className="glass rounded-xl p-5"><div className="mb-5 flex items-center justify-between"><div><p className="text-sm font-semibold text-white">System signals</p><p className="mt-1 text-xs text-slate-500">Live health indicators</p></div><span className="rounded-full bg-emerald-400/10 px-2 py-1 text-[10px] font-bold text-emerald-300">4 NOMINAL</span></div><Signal icon={<Thermometer />} label="GPU thermals" value="67°C" sub="Within operating range" tone="emerald" /><Signal icon={<Cpu />} label="Worker heartbeat" value="99.98%" sub="32 / 32 responding" tone="emerald" /><Signal icon={<AlertTriangle />} label="Anomalies" value="01" sub="Agent watching Node-04" tone="amber" /></div></section><section className="mt-6 glass overflow-hidden rounded-xl"><div className="flex items-center justify-between border-b border-slate-800 px-5 py-4"><div><p className="text-sm font-semibold text-white">Active render queue</p><p className="mt-1 text-xs text-slate-500">Jobs currently flowing through the farm</p></div><a href="/dashboard/incidents" className="flex items-center gap-1 text-xs font-semibold text-amber-400 hover:text-amber-300">View audit trail <ArrowUpRight size={14} /></a></div><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-xs"><thead className="bg-slate-950/50 text-[10px] uppercase tracking-[.16em] text-slate-600"><tr><th className="px-5 py-3 font-semibold">Job</th><th className="px-5 py-3 font-semibold">Engine</th><th className="px-5 py-3 font-semibold">Status</th><th className="px-5 py-3 font-semibold">Output</th><th className="px-5 py-3 font-semibold">Node</th><th className="px-5 py-3 font-semibold">Progress</th><th className="px-5 py-3"></th></tr></thead><tbody className="divide-y divide-slate-800/70">{jobs.map((job) => <tr key={job.id} className="transition hover:bg-slate-800/20"><td className="px-5 py-4"><p className="font-semibold text-white">{job.title}</p><p className="mt-1 font-mono text-[10px] text-slate-600">{job.id}</p></td><td className="px-5 py-4 text-slate-400">{job.engine}</td><td className="px-5 py-4"><Status status={job.status} /></td><td className="px-5 py-4 text-slate-400">{job.resolution}<br /><span className="text-[10px] text-slate-600">{job.frames} frames</span></td><td className="px-5 py-4 font-mono text-slate-400">{job.node}</td><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="h-1.5 w-20 overflow-hidden rounded-full bg-slate-800"><div className={`h-full rounded-full ${job.status === 'FAILED' ? 'bg-rose-400' : 'bg-emerald-400'}`} style={{ width: `${job.progress}%` }} /></div><span className="font-mono text-[10px] text-slate-500">{job.progress}%</span></div></td><td className="px-5 py-4 text-slate-600"><ChevronRight size={16} /></td></tr>)}</tbody></table></div></section></div>;
}
function Stat({ icon, label, value, delta, tone }: { icon: React.ReactNode; label: string; value: string; delta: string; tone: string }) { return <div className="glass rounded-xl p-5"><div className="flex items-center justify-between"><span className={`text-${tone}-400`}>{icon}</span><span className="text-[10px] font-semibold text-emerald-400">LIVE</span></div><p className="mt-5 text-2xl font-bold text-white">{value}</p><p className="mt-1 text-xs font-semibold text-slate-400">{label}</p><p className="mt-3 text-[10px] text-slate-600">{delta}</p></div>; }
function Signal({ icon, label, value, sub, tone }: { icon: React.ReactNode; label: string; value: string; sub: string; tone: string }) { return <div className="flex items-center gap-3 border-t border-slate-800 py-4 first:border-t-0 first:pt-0 last:pb-0"><span className={`flex h-8 w-8 items-center justify-center rounded-lg bg-${tone}-400/10 text-${tone}-400`}>{icon}</span><div className="flex-1"><p className="text-xs font-semibold text-slate-300">{label}</p><p className="mt-0.5 text-[10px] text-slate-600">{sub}</p></div><span className="font-mono text-sm text-white">{value}</span></div>; }
function Status({ status }: { status: string }) { const style = status === 'FAILED' ? 'bg-rose-400/10 text-rose-300' : status === 'REMEDIATED' ? 'bg-amber-400/10 text-amber-300' : status === 'COMPLETED' ? 'bg-emerald-400/10 text-emerald-300' : status === 'RENDERING' ? 'bg-sky-400/10 text-sky-300' : 'bg-slate-700/50 text-slate-400'; return <span className={`rounded-full px-2 py-1 text-[10px] font-bold tracking-wide ${style}`}>{status}</span>; }
