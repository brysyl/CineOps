'use client';

import { ArrowUpRight, CheckCircle2, Clock3, DollarSign, Filter, Search } from 'lucide-react';
import { incidents } from '@/lib/data';

export default function IncidentsView() { return <div className="mx-auto max-w-[1400px] animate-float-in"><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[.2em] text-emerald-400"><CheckCircle2 size={14} /> Audit trail</div><h1 className="text-3xl font-bold tracking-tight text-white sm:text-4xl">Incident history</h1><p className="mt-2 text-sm text-slate-500">Every autonomous action, explained and accounted for.</p></div><div className="flex gap-3"><div className="flex items-center gap-2 rounded-lg border border-slate-800 bg-slate-900 px-3 py-2 text-xs text-slate-500"><Search size={14} /> Search incidents</div><button className="flex items-center gap-2 rounded-lg border border-slate-800 bg-slate-900 px-3 py-2 text-xs font-semibold text-slate-300"><Filter size={14} /> Filter</button></div></div><div className="mb-6 grid gap-4 sm:grid-cols-3"><Summary icon={<CheckCircle2 />} label="Incidents resolved" value="1,284" detail="99.8% auto-resolved" tone="emerald" /><Summary icon={<Clock3 />} label="Mean resolution time" value="1.8s" detail="-42% this month" tone="amber" /><Summary icon={<DollarSign />} label="Estimated downtime saved" value="$42.8k" detail="Across 312 interventions" tone="sky" /></div><section className="glass overflow-hidden rounded-xl"><div className="flex items-center justify-between border-b border-slate-800 px-5 py-4"><div><p className="text-sm font-semibold text-white">Recent interventions</p><p className="mt-1 text-xs text-slate-500">A searchable record of autonomous operations</p></div><span className="font-mono text-[10px] text-slate-600">LAST 30 DAYS</span></div><div className="divide-y divide-slate-800/70">{incidents.map((incident) => <article key={incident.id} className="p-5 transition hover:bg-slate-800/20"><div className="flex flex-col gap-5 xl:flex-row xl:items-start"><div className="flex flex-1 gap-4"><span className={`mt-1 flex h-9 w-9 shrink-0 items-center justify-center rounded-lg ${incident.severity === 'WARNING' ? 'bg-amber-400/10 text-amber-400' : 'bg-emerald-400/10 text-emerald-400'}`}><CheckCircle2 size={18} /></span><div><div className="flex flex-wrap items-center gap-3"><h2 className="text-sm font-semibold text-white">{incident.title}</h2><span className="rounded-full bg-emerald-400/10 px-2 py-1 text-[9px] font-bold tracking-widest text-emerald-300">{incident.severity}</span></div><p className="mt-2 text-xs leading-5 text-slate-500">{incident.cause}</p><div className="mt-3 flex flex-wrap gap-x-5 gap-y-2 text-[10px] text-slate-600"><span>Incident <b className="font-mono text-slate-400">{incident.id}</b></span><span>Node <b className="font-mono text-slate-400">{incident.node}</b></span><span>{incident.createdAt}</span></div></div></div><div className="min-w-[280px] rounded-lg border border-slate-800 bg-slate-950/50 p-4"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-slate-600">Action taken</p><p className="mt-2 text-xs leading-5 text-slate-400">{incident.action}</p><div className="mt-3 flex items-center justify-between border-t border-slate-800 pt-3 text-[10px]"><span className="text-slate-600">Resolved in <b className="font-mono text-slate-300">{incident.duration}</b></span><span className="font-semibold text-emerald-300">{incident.saved} saved</span></div></div><button className="hidden self-center text-slate-600 transition hover:text-amber-400 xl:block"><ArrowUpRight size={17} /></button></div></article>)}</div></section></div>; }
function Summary({ icon, label, value, detail, tone }: { icon: React.ReactNode; label: string; value: string; detail: string; tone: string }) { return <div className="glass rounded-xl p-5"><span className={`text-${tone}-400`}>{icon}</span><p className="mt-4 text-2xl font-bold text-white">{value}</p><p className="mt-1 text-xs font-semibold text-slate-400">{label}</p><p className="mt-3 text-[10px] text-slate-600">{detail}</p></div>; }
